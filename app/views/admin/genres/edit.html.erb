<p>ジャンル編集</p>

  <%= form_with model:@genre,url:admin_genre_path,local:true do |f| %>

  <div class="field">
    <%= f.label :name,"ジャンル名" %>
    <%= f.text_field :name %>
  </div>
  <%= f.submit"変更を保存" %>

  <% end %>
